import HomePage from "../app/audiophiles/HomePage";

export default function Home() {
  return (
      <main>
        <HomePage />
        
        {/* flex min-h-screen w-full max-w-3xl flex-col 
        items-center justify-between py-32 px-16 bg-white
        dark:bg-black sm:items-start"
        //  flex min-h-screen items-center justify-center" */}
      </main>
  );
}
